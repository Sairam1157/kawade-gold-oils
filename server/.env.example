# SMTP settings for server email sending
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Destination email that should receive contact form messages

# If you want to use EmailJS server API instead of SMTP, set these values:
EMAILJS_SERVICE_ID=service_uflwitr
EMAILJS_TEMPLATE_ID=template_u4h9znd
EMAILJS_USER_ID=D4-CmYtU6jCnmf6KU
# Optional: private key (if your EmailJS setup requires it)
EMAILJS_PRIVATE_KEY=02bMIUyky5-LKj27bkRU8

# (If you were using SMTP, keep SMTP_* variables. They are optional now.)
TO_EMAIL=kawadegoldoil@gmail.com

# Example for Gmail (not recommended for production without App Passwords):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=your_app_password
